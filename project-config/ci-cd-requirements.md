# CI/CD & Build Requirements

## Build Commands
- Initial: `npm install` or `pip install -r requirements.txt`
- Build: `npm run build`

## Test Commands
- Unit Tests: `pytest` or `npm test`
- Coverage Goal: > 80%

## Deployment Notes
- Use Vercel for automatic deployments on push to `main`.
